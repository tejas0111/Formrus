{
  "name": "formrus",
  "private": true,
  "version": "0.1.0",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @formrus/web",
    "build": "npm run build --workspace @formrus/web",
    "preview": "npm run preview --workspace @formrus/web",
    "typecheck": "npm run typecheck --workspace @formrus/web"
  }
}
